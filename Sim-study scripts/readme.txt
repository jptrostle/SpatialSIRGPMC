Run the "_datagen.r" scripts first. They produce both the simulated datasets as well as run the emulator prep.

For sim1, which is the constant beta simulation study, there are two additional versions in the text with p = 0.75 and nu = 10. They are identical codes / models except where noted.

"gamma" in the scripts are "eta" in the manuscript, the recovery rate